# The sign-in server this app sends one-time-code requests to.
# A bare origin: scheme + host (+ port). No path, no trailing slash needed.
# Leave it unset and the card says "No sign-in server is set" and sends nothing.
VITE_SIGNIN_ORIGIN=
